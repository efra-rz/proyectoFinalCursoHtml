block config
  include ../config/config.pug

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title #{pageTitle} | #{siteTitle}
    meta(name='description' content=description)
  body
  
